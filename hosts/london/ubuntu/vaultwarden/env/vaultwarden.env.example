# Store the real file at secrets/vaultwarden/london/vaultwarden.env.
# Prefer a hashed ADMIN_TOKEN and keep the plaintext token only in pass.
ADMIN_TOKEN='$argon2id$v=19$m=65540,t=3,p=4$replace$replace'
INVITATIONS_ALLOWED=false
SIGNUPS_ALLOWED=false
