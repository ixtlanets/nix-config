# Local MQTT broker for Home Assistant <-> Frigate.
# Listens on localhost only from the host's point of view (docker port mapping
# binds 127.0.0.1). Frigate reaches the broker over the shared "hamqtt" docker
# network, where this listener also accepts connections from containers.
listener 1883
allow_anonymous true
