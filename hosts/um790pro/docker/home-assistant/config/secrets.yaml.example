# Template for config/secrets.yaml (gitignored, never committed).
# Reference secrets from any tracked yaml with: !secret <key>
# (Nothing consumes secrets.yaml yet — the frigate integration is
# UI-configured and does not use !secret. Add keys here when needed.)

